<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.music.audioplayer">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".EqualizerActivity"></activity>
        <activity android:name=".ApplicationThemeActivity" />
        <activity android:name=".ThemeActivity" />
        <activity android:name=".ColorSettingActivity" />
        <activity android:name=".SingleRowStyleActivity" />
        <activity android:name=".Demo" />
        <activity android:name=".FolderHigherciSongDisplayActivity" />
        <activity android:name=".SongInfoActivity" />
        <activity android:name=".FolderSongDisplayActivity" />
        <activity android:name=".SettingActivity" />
        <activity android:name=".MusicControlActivity" />
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".ForegroundService" />
    </application>

</manifest>